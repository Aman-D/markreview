{
  "name": "@markreview/model",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "The Review aggregate: append-only, immutable, owns the state machine and merge(). Pure.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "dependencies": {
    "@markreview/anchor": "workspace:*",
    "@markreview/schema": "workspace:*"
  },
  "markreview": {
    "layer": "core"
  }
}
