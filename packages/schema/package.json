{
  "name": "@markreview/schema",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "JSON Schema + validate() + migrate() for the .review.json format. Single source of truth.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "dependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1"
  }
}
